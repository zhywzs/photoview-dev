# PhotoView 网格缩放 / 时间线 需求文档（待审阅）

> 目的：把过去若干轮沟通里你提出的要求固化下来，作为实现与验收的唯一依据。
> 本文档只描述"要什么"，不描述"怎么实现"。请直接在需要修改的地方批注。

---

## 0. 总目标

- 面向移动端的照片时间线/相册网格。
- **性能与流畅度优先级最高**；网格缩略图质量可以让步（看细节靠点开大图）。
- 任何缩放/切换过程中**不允许屏闪、不允许明显卡顿**。

---

## 1. 时间线朝向与初始位置

1. 最新照片在**右下角**。
2. 进入时间线时，初始位置在**最底部**（即最新照片可见）。
3. 向上滚动 = 日期变早（越往上越旧）。
4. 首次进入后，在用户**主动滚动之前**，位置必须稳定停在底部，不因数据加载/排版/分页而跳动。
5. 进入时间线不应触发整库刷新（不得出现因 `resetStore` / 重复 refetch 导致的跳动）。

---

## 2. 缩放挡位

1. 挡位为离散的 **3 / 5 / 15 / 30**（列数）。
2. 缩放只在这些挡位之间切换，不存在中间挡位。
3. 松手后必须吸附到最近的挡位。

---

## 3. 缩放的视觉与交互

### 3.1 缩放中心
1. 缩放的**中心是手势中心**（双指中点），横纵两个方向都以它为中心。
2. 手指下的内容在缩放过程中保持在被按位置（锚点不漂移）。

### 3.2 缩放过程
3. 缩放过程要**跟手**（由手势驱动），并且**可回退**（手往回收，画面跟着退）。
4. 缩放过程中**只允许容器变化 + 图片各自独立加载**；不允许整屏刷新。
5. 缩放过程中允许用 **CSS 缩放**（图片短暂略糊可以接受，松手后变清晰）。
6. 可以接受"动画中部分图片是未加载的白色"，但**不接受卡顿和频闪**。
7. 可以接受整体的小幅位移；**不接受大幅度乱动**。

### 3.3 跨挡过渡
8. 一次跨挡过程中，**只能出现一次图片转换（淡入淡出）**，不能出现多次。
   - 反例：5→15 的过程中间反复淡入淡出多次。
9. 淡入淡出要**跟随手势**；手势停住时，淡化应继续叠加、最终完成。
10. 跨挡最快约 **500ms**；手势更慢时，过渡以手势为准（不超过该速率）。
11. 允许一次手势跨多档，但要有**滑动阈值（滞后）**，避免在挡位边缘来回跳。
12. 多档切换需**串行**（一段完成后才进入下一段）。

### 3.4 新出现的图片
13. 跨挡时"新出现"的图片，必须**提前计算好它应该在的位置**，直接出现在该位置，**不参与淡入淡出**。
14. 新图片应从**屏幕外**随缩放自然地进入。
15. 参考 `animation.xlsx`：中心 3×3 是当前挡位（3），外圈是下一挡位（5）新出现的图片。
16. **不允许出现"容器排序变化"**（照片在格子之间乱跳/整块重排）。

### 3.5 左右对称
17. 缩小时，新增的容器应**左右两侧同时出现**；放大时左右两侧同时收回。
   - 参考 `animation.xlsx`：不是只在右侧增减。
18. 缩放的整体中心应由**手势中心决定**。

> ⚠️ 注意：第 1 条（最新固定右下，行按时间线自然分页）与第 17/18 条（围绕手势左右对称增减）在几何上存在冲突，见第 7 节待决策。

### 3.6 垂直方向
19. 上下方向若出现新图片，优先**复用原有图片**并做淡入淡出（而不是像左右那样完全新增）。
20. 跨挡后若不再显示的外围图片：直接卸载。

---

## 4. 图片加载

1. 缩略图统一（不因缩放而切换图源、重新请求）。
2. 新出现图片可**一次性发起请求、流式显示**（允许先白后出）。
3. 每个 tile 各自独立加载，互不阻塞。

---

## 5. 删除交互（已完成，作为回归项）

1. 长按网格 tile，或在大图查看器里删除 → 图片有"掉落/淡出"动画。
2. 删除后从 Apollo 缓存本地移除，**不触发整屏刷新**；剩余图片平滑移动填补空位。
3. 删除进入回收站，30 天内可恢复。

---

## 6. 其它既有功能（回归项）

1. 上传（FAB → `/api/upload`，上传后触发扫描）。
2. 回收站（恢复 / 永久删除 / 清空）。
3. 存储统计可视化。
4. 搜索增强。
5. 悬浮日期胶囊（当前挡位滚动时显示日期）。
6. 部署：WSL2 + systemd，无 Docker，任意局域网 IP 访问。

---

## 7. 待你决策的核心冲突（必须先定，否则无法同时满足）

**冲突**：
- 需求 1："最新固定右下、行按时间线自然分页" → 网格的每一行起点是固定的。
- 需求 17/18："围绕手势中心、左右两侧同时增减" → 必须把整行围绕手指那张照片**重新换行**（行起点随手指浮动）。

两者**几何上互斥**：一旦为了左右同时生长而围绕手指换行，"最新"就不再固定在右下角；而且换行起点会随手势移动（这正是一段时间里"位置乱动"的根源）。

**可选项**：
- **方案 A（稳定，当前实现）**：最新固定右下、行首固定；列只从**右侧**增减。缩放平稳、无跳动，但不满足"左右同时"。
- **方案 B（贴合 animation.xlsx）**：行围绕手势换行，左右同时增减；但行首随手指浮动，"最新"不再固定右下，且需要一次平滑"归位"动画来收尾。

> 请在此处勾选：方案 A ☐ / 方案 B ☐ / 其它（请描述）☐

---

## 8. 验收清单（逐条可测）

- [ ] 进入时间线稳定停在最新（右下），无跳动。
- [ ] 缩放中心 = 手势中心（横纵皆是）。
- [ ] 一次跨挡只有一次淡入淡出。
- [ ] 淡入淡出跟手、可回退、停手会继续完成。
- [ ] 跨挡最快 ~500ms，慢则跟手。
- [ ] 新出现的图片直接出现在预计算位置，不参与过渡，从屏幕外进入。
- [ ] 无"容器排序变化"。
- [ ] 缩放过程中无屏闪、无卡顿。
- [ ] 右图为最新，向上日期变早。
- [ ] 删除：掉落动画 + 局部填补，无整屏刷新。
- [ ] （方案 B 时）左右两侧同时增减、行首归位平滑。

---

## 9. 当前实现状态（供参考）

- 已实现：离散挡位 3/5/15/30、一次跨挡一次淡化、以手势为中心、图片独立加载、统一 atlas、删除局部更新、时间线倒置（最新右下、初始底部）、进入时间线贴底不跳。
- 未满足：**左右两侧同时增减**（现为右侧增减），即第 7 节方案 A/B 的取舍未定。
