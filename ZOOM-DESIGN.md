# 时间线网格缩放 — 方案选型与设计文档

> 目标：在"可以用任意前端架构"的前提下，给出能同时满足 `ZOOM-REQUIREMENTS.md` 的
> 技术选型与实现设计。本文档只谈方案，不含具体代码。

---

## 1. 先把"物理约束"讲清楚（决定方案边界）

时间线是一条**线性序列**（旧的在前、新的在后）。把线性序列放进网格，只有两种自洽模型：

| 模型 | 行分页 | 缩放时新增内容方向 | 最新位置 |
|---|---|---|---|
| **自然分页** | 行首固定，永远从序列固定位置分行 | 只在序列"前方"（更旧）一侧增长 | 固定最右/最下 |
| **围绕锚点换行** | 行首随手势那张照片浮动 | 左右/上下**同时**增长 | 不再固定 |

结论：
- **"最新固定右下" + "左右同时增减" 无法同时成立**（这是线性序列的数学性质，不是实现问题）。
- 但可以做到：**初始/静止状态最新在右下；手势中围绕锚点换行、左右同时增减；松手后保持换行不跳动**。
  代价仅是"缩放后最新不再严格钉在右下"，但整体顺序与观感一致。
- 另一个必然：**在序列两端（最新/最旧）附近，只有一侧有内容**。这是物理极限，任何方案都一样。

**本设计的取舍**：采用"围绕锚点换行"，因为它满足你最核心的 `animation.xlsx` 观感；
同时通过"容器位置永不重排、只做缩放的方案"，从根上消除乱动。

---

## 2. 技术选型

### 2.1 渲染层（核心决策）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| React + DOM/CSS | 复用现有代码、可访问性好 | 每帧改 N 个 DOM 样式、布局/重排风险、事件与滚动耦合，正是过去卡顿/乱动的来源 | 只用于静止骨架 |
| **Canvas 2D（推荐）** | 每帧只画可见 tile；`drawImage` 直接切 atlas；逐 tile 淡入淡出=两次 drawImage+alpha；不产生布局，天然无"重排"；命中测试自行计算 | 需自建渲染循环/命中测试/可访问性 | **主选** |
| WebGL（PixiJS） | 极限性能、几万 sprite 无压力 | 依赖重、包体大、调试成本高 | 数据量再上一个量级时再上 |

**选 Canvas 2D**：可见 tile 数最多约 30 列 × 屏高 ≈ 几百~1000 个，`drawImage` 完全够用；
且我们只在手势期间高频重绘，静止时停帧。

### 2.2 其余选型
- **交互**：原生 Pointer/Touch/Wheel + 自建手势状态机（pinch/wheel/tap/long-press），
  不引入手势库（避免与自研惯性/吸附冲突）。
- **状态**：一个独立的 `GridEngine`（纯 JS，非 React）持有序列、布局、缩放进度；
  手势期间**完全不触发 React 渲染**。React 只负责页头、日期胶囊、查看器等外围 UI。
- **数据**：沿用 Apollo/GraphQL；缩略图沿用服务端 256atlas（sprite sheet）——
  Canvas 2D 直接按 tile 切片 `drawImage`，天然就是 sprite 渲染。
- **动画**：单一 `requestAnimationFrame` 主循环，手势/惯性/淡化都由它驱动。
- **图像加载**：atlas 用 `Image` 预加载并按 URL 去重缓存；未就绪的 tile 画白/画占位，
  不阻塞其它 tile（符合"允许白块"）。
- **高 DPI**：canvas 尺寸按 `devicePixelRatio` 放大，保持清晰。

---

## 3. 架构分层

```
┌───────────────────────────────────────────────┐
│ React chrome: Header / 日期胶囊 / 查看器 / 菜单   │
├───────────────────────────────────────────────┤
│ GridEngine (纯 JS, 无 React)                    │
│  ├─ SequenceModel   有序照片序列 + atlas 元数据  │
│  ├─ LayoutEngine    序列窗口 → 槽位几何/照片映射  │
│  ├─ ZoomModel       挡位/进度/淡化/吸附/惯性      │
│  └─ InputFSM        手势状态机（pinch/wheel/tap） │
├───────────────────────────────────────────────┤
│ CanvasRenderer      每帧按 LayoutEngine 绘制     │
│  （可见 tile：缩放 + 源/目标图交叉淡化）          │
├───────────────────────────────────────────────┤
│ ImageStore          atlas/缩略图加载与缓存       │
└───────────────────────────────────────────────┘
```

要点：**布局与渲染分离、渲染与 React 分离**。缩放过程中 React 完全不参与。

---

## 4. 布局模型（解决"左右对称 + 不乱动"的核心）

### 4.1 基本量
- 有序序列 `S[0..N-1]`（`S[0]` 最旧，`S[N-1]` 最新）。
- 视觉 tile 尺寸 `T`；间距 `g = T·R`（R≈0.06）；步距 `P = T + g`。
- **槽位 `(r, c)` 永远位于 `(c·P, r·P)`**：槽位是关键，它**只随 P 缩放，永不重排**。
- 槽位里的照片 = `S[W + r·C + c]`，其中 `C` 列数、`W` 为"换行原点"（整数）。
  越界（<0 或 ≥N）即空位。

### 4.2 静止状态 `(C₀, W₀)`
- 初始：`W₀ = 0`（自然分页，`S[N-1]` 在最后一格=右下），滚动到最底。
- **滚动只改滚动位置，不改 `W₀`** → 行对齐稳定，不会一边滚一边重排。

### 4.3 手势中的换行（关键）
1. 取手指下那张照片 `a = S[W₀ + r_a·C₀ + c_a]`（`r_a,c_a` 是手指所在槽位）。
2. 目标挡位 `C₁`。
3. 令 `W₁ = a - (r_a·C₁ + c_a)`：使**同一张照片 `a` 在目标布局里仍落在同一个槽位 `(r_a,c_a)`**。
4. 目标布局：槽位 `(r,c)` 显示 `S[W₁ + r·C₁ + c]`；没有源的列（`c ≥ C₀`）**直接显示目标照片、从屏幕外滑入**。
5. 完成后：`(C₀,W₀) ← (C₁,W₁)`。

因为**只有 P 在变、槽位坐标不变**，画面上唯一的运动是"整块绕手指均匀缩放"；
照片内容的变化全部由**一次交叉淡化**承担。→ 不会有"位置乱动"，也不会有多次转换。

### 4.4 左右/上下同时增长从哪来
`W` 的平移使同一槽位的照片整体"向前/向后"错位，于是：
- 缩放时，**左侧和右侧的槽位会同时从屏幕边缘进入/退出**（因为 `W₁` 把序列在行内左右铺开）；
- 在序列两端附近，只有一侧有内容（物理极限，符合预期）。

### 4.5 文档/滚动映射
- 序列最旧一行 `rowMin = ⌊-W / C⌋`，最新一行 `rowMax = ⌊(N-1-W) / C⌋`。
- 内容高度 = `(rowMax - rowMin + 1) · P_code`（**用已提交挡位的 P**，保证文档高度不随手势抖动）。
- 槽位文档 Y = `(r - rowMin) · P`；整块再做一个 Y 平移，把锚点槽位稳定在手指处；
  吸附结束时把这个平移折算进滚动。

### 4.6 与 `animation.xlsx` 的对应
- 手指所在照片固定在中心（= 3×3 的中心）；
- 相邻挡位新出现的照片排在四周（= 外圈 5），从屏幕外滑入；
- 结束后整体吸附成整齐网格。

---

## 5. 缩放与过渡时间线（ZoomModel）

状态：`progress`（0=源挡，1=目标挡）、`visualTile`（连续尺寸）、吸附目标。

- **跟手**：`visualTile = startTile · (手指间距 / 起始间距)`，横纵以双指中点为原点，
  通过"锚点槽位固定在手指处"实现（不改滚动，只改整块平移）。
- **触发换挡**：`visualTile` 越过相邻挡位几何中点一定阈值才触发（**滞后**，防抖）。
- **一次淡化**：`fade = clamp((progress - 0.6) / 0.4, 0, 1)`，在进度末段执行**一次**交叉淡化。
  新列/新行没有源图，直接 100% 显示（不淡化）。
- **速率上限**：`|d(fade)/dt| ≤ 1/500ms`；手快则封顶，手慢则跟手。
- **停手叠加**：手指 110ms 未移动即视为停手，`fade` 继续以该速率走到端点，保证完成。
- **可回退**：反向移动时 `progress` 回退，淡化反向播放，越过阈值则撤销。
- **松手吸附**：把 `visualTile` 用缓动吸附到最近挡位，并把锚点平移折算进滚动。
- **多档串行**：一段完成后再判定是否进入下一段（同样有阈值）。

---

## 6. 性能策略

1. **零 React 渲染**：手势期间只跑 `GridEngine + Canvas`，React 不重渲染。
2. **只画可见槽位**：按视口算出 `r,c` 范围（含 overscan），其余不画。
3. **sprite 切片**：atlas 一次加载，`drawImage(src, sx,sy,sw,sh, dx,dy,dw,dh)` 直接出图。
4. **交叉淡化 = 两次 drawImage + globalAlpha**，无额外 DOM/滤镜。
5. **停帧**：无手势/无惯性/无加载变化时不重绘。
6. **图像缓存**：以 URL 为 key，`onload` 后置 `dirty` 触发一次重绘。
7. **DPR 缩放**：canvas backing store 按 DPR，逻辑坐标不变。
8. **虚拟化命中测试**：`pointer → 槽位 → 照片` 由 LayoutEngine 反算，O(1)。

---

## 7. 可访问性与交互细节

- 命中测试走 LayoutEngine，支持点击打开、长按删除（保留现有交互）。
- 可访问性：在 canvas 之上叠一层**透明的 DOM 按钮**（仅可见槽位），提供键盘/读屏与悬停收藏；
  或接受 canvas-only（详情在查看器里）。
- 滚动：使用窗口滚动（保留浏览器原生滚动与惯性）；缩放时冻结滚动、用平移补偿。
- 日期胶囊：由 GridEngine 报告可见的序列区间 → React 更新胶囊文案。

---

## 8. 迁移路径（低风险分阶段）

1. **阶段 1**：抽出 `LayoutEngine`（纯函数）+ `ZoomModel`（纯状态机），单元测试覆盖
   挡住、阈值、吸附、淡化曲线、换行原点计算。此时仍用现有 DOM 渲染验证数学。
2. **阶段 2**：接入 `CanvasRenderer`，用同一 `LayoutEngine` 绘制；保留 DOM 版做对拍。
3. **阶段 3**：切到 canvas 为主，移除 DOM 网格；补命中测试与可访问层。
4. **阶段 4**：调优（预取、DPR、停帧、惯性）。

好处：核心数学先可测（不再靠肉眼看效果反复返工），渲染可替换。

---

## 9. 风险与取舍

| 项 | 说明 | 缓解 |
|---|---|---|
| 两端单侧增长 | 线性序列物理极限 | 明确接受；两端用空白/留白处理 |
| 换行原点浮动 | 缩放后"最新"不再钉右下 | 官方定义为"初始/静止在底部；缩放围绕手指" |
| canvas 可访问性 | 读屏/键盘 | 透明 DOM 叠加层 |
| canvas 首帧白 | 图未加载 | 允许白块；atlas 预取 |
| 纯 canvas 改动大 | 需重写网格渲染 | 分阶段、数学先行、DOM 对拍 |

---

## 10. 推荐结论（一句话）

用 **自研 `GridEngine`（纯 JS）+ Canvas 2D sprite 渲染**，布局采用"**槽位永不重排、只缩放 +
按锚点换行 + 一次交叉淡化**"模型：这是唯一能同时满足"跟手、无卡顿、无频闪、只有一次转换、
新图预置、左右同时增减"的架构；DOM 版仅保留为骨架与对拍。
```
```
